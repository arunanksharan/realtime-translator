# 🔧 Fixed Daily.co Audio Controls Type Error\n\n## ✅ **Issue Resolved:**\n\n**Error**: \n```\nProperty 'localVideo' is missing in type '{ joinCall: () => Promise<void>; ... }' but required in type '{ callState: \"error\" | \"idle\" | \"joining\" | \"joined\" | \"left\"; localAudio: boolean; localVideo: boolean; ... }'\n```\n\n## 🎯 **Root Cause:**\nType mismatch between:\n- `useDaily` hook return type (missing `localVideo`)\n- `AudioControlsCard` component expected props interface\n- Function signature mismatch (async vs sync functions)\n\n## 🔧 **Fixes Applied:**\n\n### **1. Added Missing `localVideo` Property**\n**File**: `src/hooks/use-daily.ts`\n```typescript\n// Added to return statement:\nlocalVideo: dailyConnection.localVideo,\n```\n\n### **2. Fixed Function Signatures**\n**File**: `src/components/AudioControlsCard.tsx`\n```typescript\n// Updated interface to match actual async functions:\ntoggleMicrophone: () => Promise<void>\nsetMicrophoneVolume: (volume: number) => Promise<void>\nsetSpeakerVolume: (volume: number) => Promise<void>\n```\n\n### **3. Updated Event Handlers**\n**File**: `src/components/AudioControlsCard.tsx`\n```typescript\n// Added proper async handling:\nonClick={() => daily.toggleMicrophone().catch(console.error)}\ndaily.setMicrophoneVolume(volume).catch(console.error)\ndaily.setSpeakerVolume(volume).catch(console.error)\n```\n\n### **4. Created Unified Type Definitions**\n**File**: `src/types/index.ts`\n```typescript\nexport interface DailyHookReturn {\n  joinCall: () => Promise<void>\n  leaveCall: () => Promise<void>\n  toggleMicrophone: () => Promise<void>\n  setMicrophoneVolume: (volume: number) => Promise<void>\n  setSpeakerVolume: (volume: number) => Promise<void>\n  getNetworkStats: () => Promise<any>\n  callState: DailyCallState['callState']\n  participants: DailyCallState['participants']\n  localAudio: DailyCallState['localAudio']\n  localVideo: DailyCallState['localVideo']\n  networkQuality: DailyCallState['networkQuality']\n}\n```\n\n### **5. Applied Type Safety**\n**Files**: `src/hooks/use-daily.ts` & `src/components/AudioControlsCard.tsx`\n```typescript\n// Hook now returns proper type:\nexport function useDaily(sessionTokens: SessionTokens | null): DailyHookReturn\n\n// Component uses unified type:\ninterface AudioControlsCardProps {\n  daily: DailyHookReturn\n  className?: string\n}\n```\n\n## ✅ **Result:**\n\n- ✅ **TypeScript compilation successful**\n- ✅ **Type safety maintained throughout**\n- ✅ **Async function handling properly implemented**\n- ✅ **No missing properties**\n- ✅ **Consistent type definitions**\n\n## 🎯 **Benefits:**\n\n1. **Type Safety**: All interfaces now properly match implementation\n2. **Error Handling**: Async functions have proper error handling\n3. **Maintainability**: Unified type definitions prevent future mismatches\n4. **IntelliSense**: Better IDE support with accurate type information\n5. **Future-Proof**: Changes to Daily hook automatically propagate to components\n\n---\n\n**The session page should now build successfully without TypeScript errors! 🎉**\n